<div class="btns">
	<a href="mailto:hello@jonnyjones.dev" class="btn">
		<span>hello@jonnyjones.dev</span>
	</a>
	<a href="#" class="btn">
		<span>Codepen</span>
	</a>
	<a href="#" class="btn">
		<span>CV</span>
	</a>
	<a href="#" class="btn">
		<span>Linkedin</span>
	</a>
</div>

<style lang="scss">
	.btns {
		gap: 1rem;
		position: fixed;
		bottom: 2rem;
		left: 50%;
		transform: translateX(-50%);
		display: flex;

		display: none;
	}

	.btn {
		padding: 0.5rem 1rem;
		text-transform: uppercase;
		text-decoration: none;
		color: var(--white);
		position: relative;
		border-radius: 9999px;
		background-color: var(--black);
		transition: 500ms width;
		width: 50px;

		span {
			opacity: 0;
			transition: 300ms opacity;
		}
	}

	.btn::before {
		content: "EMAIL";
		display: block;
		position: absolute;
		left: 50%;
		transform: translateX(-50%);
		z-index: 3;
		color: black;
		opacity: 1;
		transition: 150ms opacity;
	}

	.btn::after {
		content: "";
		background-color: var(--grey);
		display: block;
		width: 100%;
		height: 100%;
		position: absolute;
		top: 0;
		left: 0;
		border-radius: 9999px;
		transition: 600ms;
	}

	.btn:hover {
		width: 225px;

		span {
			opacity: 1;
			margin-left: 1rem;
		}

		&::before {
			opacity: 0;
		}

		&::after {
			transform: scale(0.2);
			width: 45px;
		}
	}
</style>
